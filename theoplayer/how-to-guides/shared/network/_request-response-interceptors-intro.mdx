A _request interceptor_ runs before the player opens the connection, so it can change properties of the
request such as its URL, method, headers or body. It can also respond to the request immediately,
without contacting the server.

A _response interceptor_ runs before the player consumes the response, so it can change the status,
the headers or the body of the response.
