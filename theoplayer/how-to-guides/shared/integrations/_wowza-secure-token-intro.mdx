Wowza Streaming Engine™ offers a SecureToken version 2 module,
which provides playback protection through a security token that is exchanged between the server and its clients.
The SecureToken is a challenge/response system that helps to protect content against spoofing threats.
Each connection is protected by a random key and a password (shared secret).
