name: Lint

on:
  workflow_call:
    inputs:
      node_version:
        description: Define the version of Node.JS to use.
        required: false
        type: string
        default: '24'

concurrency:
  group: lint-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pr-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Create Ads OpenAPI token
        uses: actions/create-github-app-token@v3
        id: ads-token
        with:
          client-id: ${{ vars.THEOPLAYER_BOT_CLIENT_ID }}
          private-key: ${{ secrets.THEOPLAYER_BOT_PRIVATE_KEY }}
          owner: Dolby-OptiView
          repositories: optiview-ads

      - name: Checkout 🛎️
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          submodules: true

      - name: Setup Node.JS 🔧
        uses: actions/setup-node@v7
        with:
          node-version: ${{ inputs.node_version }}
          cache: 'npm'
          cache-dependency-path: ./package-lock.json

      - name: Print NPM version 🌎
        shell: bash
        run: |
          npm --version

      - name: Install dependencies 🔧
        shell: bash
        run: |
          git config --global --add safe.directory ${GITHUB_WORKSPACE}
          npm ci

      - run: npm run gen-api-docs
        env:
          GITHUB_TOKEN: ${{ steps.ads-token.outputs.token }}
      - run: npm run check-format
      - run: npm run lint
      - run: npm run typecheck
